// Scaffold placeholder — proves the build/lint/typecheck/test pipeline
// works end-to-end. Replaced by real exports (defineFlow, poll, evidence
// collectors) starting with the next implementation step.
export function version(): string {
  return '0.0.1';
}
